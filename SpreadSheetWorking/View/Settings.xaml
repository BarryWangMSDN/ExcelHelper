<Page
    x:Class="SpreadSheetWorking.View.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpreadSheetWorking.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
       
            <controls:Loading x:Name="LoadingControl" HorizontalContentAlignment="Center"
                      VerticalContentAlignment="Center">
                <controls:Loading.Background>
                    <SolidColorBrush Color="Black" Opacity="0.5" />
                </controls:Loading.Background>
                <ContentControl x:Name="LoadingContentControl" />
            </controls:Loading>
        <StackPanel>
            <Button Content="testloaddata" Click="Button_Click" Margin="0,24,0,0"/>
            <Button Content="writetosqlserver" Margin="0,24,0,0" Click="Write_DB"/>
        </StackPanel>
    </Grid>
</Page>
